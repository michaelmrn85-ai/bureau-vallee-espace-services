{
  "name": "bureau-vallee-espace-services",
  "version": "1.0.0",
  "private": true,
  "description": "Espace Services Bureau Vallee Montaigu-Vendee",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check app.js && node --check upload.js && node --check admin.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "multer": "^2.1.1"
  },
  "engines": {
    "node": ">=18"
  }
}
